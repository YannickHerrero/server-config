[package]
name = "server-onboarding"
version = "0.1.1"
edition = "2024"
rust-version = "1.95.0"
publish = false

[dependencies]
crossterm = "=0.29.0"
ratatui = { version = "=0.30.2", default-features = false, features = ["crossterm"] }

[dev-dependencies]
tempfile = "=3.27.0"
